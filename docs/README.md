# Documentation index

## Current contracts

`../AGENTS.md` governs repository work. `../TEMPLATE.md` covers reuse and setup. `LEAD-BUILD.md` covers dealer-copy work; it does not authorize editing this master into a dealer skin.

`ARCHITECTURE.md` identifies data, state, route and lifecycle owners. `COMPONENT-PATTERNS.md`, `../DESIGN.md` and `../DESKTOP_STYLE_GUIDE.md` define visual boundaries. `CONTENT-CONTRACT.md` defines shared business content and sample-data limitations. `QA.md` defines reproducible verification and release boundaries.

## Evidence and history

`refactor-2026-09-12/REPORT.md` records the current uncommitted cleanup and verification. `mobile-finalization/2026-09-12/` and `final-polish/` record earlier audits and implementation states; their counts, failures and backlog items are historical observations, not current results. `DESKTOP_STANDARDIZATION_AUDIT_2026-09-11.md` is likewise dated evidence.

`legacy/` holds source-era migration/design notes, provenance and original parent-repo instructions. The root `../design-qa.md` is also explicitly marked as superseded blue-hero evidence, not the current design. `.template/` and `REPO-SPLIT.md` preserve source lineage. Do not execute old port/branch/deployment instructions or revive superseded components from historical documents.
